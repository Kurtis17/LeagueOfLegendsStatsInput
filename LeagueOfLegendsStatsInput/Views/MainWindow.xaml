<Window x:Class="LeagueOfLegendsStatsInput.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LeagueOfLegendsStatsInput"
        mc:Ignorable="d"
        Background="DimGray"
        Title="LoL Input" Height="450" Width="1000">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="450"/>
            <ColumnDefinition Width="450"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Margin="5" Background="IndianRed">
            <StackPanel>
                <Button Content="{Binding }" Command="{Binding }" Margin="2 15 2 10"/>
                <Button Content="{Binding }" Command="{Binding }" Margin="2 2 2 10"/>
                <Button Content="{Binding }" Command="{Binding }" Margin="2 2 2 10"/>
                <Button Content="{Binding } " Command="{Binding }" Margin="2 2 2 10"/>
            </StackPanel>
        </DockPanel>

        <ContentControl Content="{Binding }" Grid.Column="1" Margin="5 5"/>
        <ContentControl Content="{Binding }" Grid.Column="2" Margin="5 5"/>

    </Grid>
    
</Window>
